<nav class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <a href="/">
                        <img class="block lg:hidden h-8 w-auto" src="{{ relative_prefix|default('.', true) }}/assets/images/header.png" alt="Ben Greenberg">
                        <img class="hidden lg:block h-8 w-auto" src="{{ relative_prefix|default('.', true) }}/assets/images/header.png" alt="Ben Greenberg">
                    </a>
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                    {%- for item in collections.all | eleventyNavigation -%}
                        {%- set isCurrent = (item.url == page.url) -%}
                        {%- set isParent = item.url != '/' and item.url in page.url -%}
                        <!-- Current: "border-indigo-500 text-gray-900", Default: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700" -->
                        <a href=
                            "{% if isCurrent %}#content{% else %}{{ item.url | url }}{% endif %}" 
                            {% if isCurrent %} aria-current="page" class="border-indigo-500 text-gray-900 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium"
                            {% else %}
                                class="border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1"
                            {% endif %} 
                        >
                            {{ item.title }}
                        </a>
                    {%- endfor -%}
                </div>
            </div>

        </div>
    </div>
     <div 
        class="sm:hidden"
        id="mobile-menu"
    >
    <div class="pt-2 pb-4 space-y-1">
      <!-- Current: "bg-indigo-50 border-indigo-500 text-indigo-700", Default: "border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700" -->
      <a href="/about" @click="isOpen = false" class="bg-indigo-50 border-indigo-500 text-indigo-700 block pl-3 pr-4 py-2 border-l-4 text-base font-medium">About</a>
      <a href="/writings" @click="isOpen = false" class="border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700 block pl-3 pr-4 py-2 border-l-4 text-base font-medium">Writings</a>
      <a href="/talks" @click="isOpen = false" class="border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700 block pl-3 pr-4 py-2 border-l-4 text-base font-medium">Talks</a>
      <a href="/projects" @click="isOpen = false" class="border-transparent text-gray-500 hover:bg-gray-50 hover:border-gray-300 hover:text-gray-700 block pl-3 pr-4 py-2 border-l-4 text-base font-medium">Projects</a>
    </div>
  </div>
</nav>